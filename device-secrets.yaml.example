# Copy to device-secrets.yaml only when devices need different credentials.
# This file is a template; device-secrets.yaml itself is excluded from Git.
devices:
  Leaf-1:
    username: admin
    password: replace-me
